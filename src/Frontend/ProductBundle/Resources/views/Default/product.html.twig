{% extends "FrontendLayoutBundle:Default:layout.html.twig" %}

{% block title%} WebShop - Kezdőlap {% endblock %}
{% block bodyClass %}class="index_page"{% endblock%}
{% block javascripts %}
	{{ parent() }}
    <script type="text/javascript" src="{{ asset('js/jquery-organictabs.js') }}"></script>
    <script type="text/javascript">
            $(document).ready(function(){
                $("#productTabs").organicTabs();
            });
    </script>
{% endblock %}
    
{% block main %}
    <div class="main">
            {% block leftSide %}                        
                {{parent()}}       
            {% endblock %}
        <div class="container">
            <div id="current-product-box">
                <div class="product_name">{{product.name}}</div>
                <div class="upper_box">
                    <div class="image_container">

                        <img src="
                            {% if (product.getProductImages()|length != 0 ) %}
                                      {{image(product.getProductImages()[0].getWebPath()).resize(200,200)}}"/>
                            {% else %}     
                                    {{ asset('images/no_image.jpg')}}"/>
                            {% endif %}

                    </div>
                    <div class="right_box">
                        <div class="product_price">Ára: {{product.getPrice()}} Ft</div>
                        <input class="to_cart_button" type="button" value="Kosárba">
                    </div>    
                </div>
            </div> 
            <div id="productTabs">
                <ul class="nav">
                    <li class="nav-one"><a href="#tabProductInformation" class="current">Specifikáció</a></li>
                    <li class="nav-two"><a href="#tabDescription">Leírás</a></li>
                    <li class="nav-two"><a href="#tabGuarantee">Garancia</a></li>
                    <li class="nav-two"><a href="#tabQuestions">Kérdésem van</a></li>
                </ul> 
                <div class="list-wrap">
                    <div class="tabContent" id="tabProductInformation">
                        {% include 'FrontendProductBundle:Default:tabProductInformation.html.twig' with {'product' : product} %}
                    </div>
                    <div class="tabContent hide" id="tabDescription">
                        {% include 'FrontendProductBundle:Default:tabProductDescription.html.twig' with {'product' : product} %}
                    </div>
                    <div class="tabContent hide" id="tabGuarantee">
                        {% include 'FrontendProductBundle:Default:tabGuarantee.html.twig' with {'product' : product} %}
                    </div>
                    <div class="tabContent hide" id="tabQuestions">
                        Kérdések
                    </div>
                </div>
            </div>
            
            
        </div> 
    </div>
{% endblock %}