frontend_add_cart:
    pattern:  /kosar/betesz
    defaults: { _controller: FrontendCartBundle:Cart:add}
    
frontend_cart:
    pattern:  /kosar
    defaults: { _controller: FrontendCartBundle:Cart:cart}
    
frontend_cart_order:
    pattern:  /kosar/rendeles
    defaults: { _controller: FrontendCartBundle:Cart:order}    